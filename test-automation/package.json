{
  "name": "test-automation",
  "private": true,
  "devDependencies": {
    "@playwright/test": "^1.40.0",
    "typescript": "^4.9.5"
  },
  "scripts": {
    "test": "playwright test",
    "test:ci": "playwright test --reporter=dot --workers=1"
  }
}
